language: python
python:
  - 3.7.4
before install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install -U pytest-cov
script:
  - pytest --cov=com.jefferson com/jefferson/test/